
# ========================
# YOLO & Machine Learning
# ========================
# ละเว้นไฟล์โมเดลพื้นฐานที่โหลดมาอัตโนมัติ
yolov8n.pt
yolov8*.pt

# ละเว้นไฟล์แคชของ Dataset ที่ YOLO สร้างขึ้น
*.cache

# ละเว้นโฟลเดอร์ runs (สำคัญมาก! เพราะข้างในจะมีรูปและกราฟเยอะมาก)
# *หมายเหตุ: หากต้องการส่งไฟล์ best.pt ให้เพื่อน แนะนำให้อัปโหลดผ่าน Google Drive หรือแยกส่งต่างหาก
runs/

# ========================
# Dataset (ข้อมูลรูปภาพ)
# ========================
# ละเว้นโฟลเดอร์รูปภาพและ Label เพื่อไม่ให้ Git เต็ม
dataset/train/
dataset/val/

# (ถ้าอยากให้อัปโหลดแค่ไฟล์ data.yaml ขึ้นไป ให้ใช้ 2 บรรทัดด้านบน 
# แต่ถ้าต้องการละเว้นโฟลเดอร์ dataset ทั้งหมดเลย ให้ใช้บรรทัดล่างบรรทัดเดียวครับ)
# dataset/